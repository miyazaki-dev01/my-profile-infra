version: "3.9"

services:
  aws-cdk:
    container_name: aws-cdk
    build:
      context: .
      dockerfile: Dockerfile
    working_dir: /app
    volumes:
      - ./:/app
    tty: true
    stdin_open: true
